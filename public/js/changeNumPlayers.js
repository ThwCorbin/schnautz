// import { v, num1to4Msg } from "./app.js";

// // Change number of players and number of cards to deal
// const changeNumPlayers = () => {
//   if (!v.activeGame) {
//     num1to4Msg.textContent === "4"
//       ? ((num1to4Msg.textContent = "3"), (v.numCards = 12), (v.numPlayers = 3))
//       : num1to4Msg.textContent === "3"
//       ? ((num1to4Msg.textContent = "2"), (v.numCards = 9), (v.numPlayers = 2))
//       : ((num1to4Msg.textContent = "4"), (v.numCards = 15), (v.numPlayers = 4));
//   }
// };

// export default changeNumPlayers;
